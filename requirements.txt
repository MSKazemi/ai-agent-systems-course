# AI Agent Systems Course — unified virtualenv
# Install once from the repo root:
#
#   python3 -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# This single venv covers all three modules (MCP, A2A, LangGraph, DeepAgent).

# ── shared ────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── MCP module ────────────────────────────────────────────────
fastmcp>=1.0.0
mcp>=1.0.0
openai>=1.0.0

# ── A2A module ────────────────────────────────────────────────
google-adk[a2a]>=0.1.0
uvicorn>=0.30.0
litellm>=1.0.0
fastapi>=0.115.0

# ── LangGraph + DeepAgent module ─────────────────────────────
deepagents>=0.4.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langchain-ollama>=0.1.0
langgraph>=0.2.0
